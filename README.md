# Image-Compressor
Image Compressor is a lightweight desktop application built using Python, Tkinter, and Pillow that enables efficient compression and format conversion of JPG and PNG images. It provides an intuitive GUI with optimized, lossless (PNG) and lossy (JPEG) compression to reduce file size while maintaining image quality across macOS systems.
